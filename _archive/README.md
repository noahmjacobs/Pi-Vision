# _archive

Files that are no longer part of the active codebase but kept for reference.

| File | What it was | Why archived |
|------|------------|--------------|
| `setup-admin.html` | One-time browser tool to create the first admin Firebase user | Already done — admin user exists |
| `setup-admin.mjs` | Node.js script for the same one-time admin setup | Already done |
| `process.py` | Headless CLI people counter (no GUI) | Replaced by `processor/app.py` which is the full desktop GUI app |

Nothing in this folder is imported or used by any active code.
